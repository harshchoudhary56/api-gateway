spring:
  application:
    name: API-GATEWAY
  data:
    mongodb:
      uri: mongodb://localhost:27017/school_erp_gateway
      database: school_erp_gateway
  # Disable Spring Boot's JDBC-based Liquibase auto-config (we use programmatic MongoDB Liquibase)
  liquibase:
    enabled: false
